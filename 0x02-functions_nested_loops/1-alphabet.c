#include "main.h"

/**
 * main - check the code
 * prints alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)

void print_alphabet(void)

{
	char ch;

	for (ch = 'a', ch <= 'z', ch++); 
	
	{
		_putchar(ch);
	}
	
	putchar('\n');

}
